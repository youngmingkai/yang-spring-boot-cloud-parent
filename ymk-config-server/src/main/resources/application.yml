server:
  port: 8080
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/youngmingkai/spring-cloud-config-repo/
          #          uri: https://github.com/youngmingkai/{application}   #  占位符
          username: xxxx
          password: xxxx

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/